# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Design system source zip (frontend/ is the unpacked version)
MyPropertyIQ Design System.zip

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Deploy bundle scratch
fyp2-deploy.tgz

# Random Forest valuation artifact (~203 MB) — exceeds GitHub's 100 MB file
# limit and is a reproducible build output of `python backend/save_models.py rf`.
# Deployed to the VM via scp, not tracked in git.
backend/artifacts/valuation_rf.joblib

# Office lock files
~$*

# Location hierarchy edge list — analysis byproduct of build_location_hierarchy.py;
# only location_hierarchy.json is loaded at runtime
processed data/location_hierarchy_edges.csv

# Environment and cache files
backend/.env
backend/.cache/

# Local context/reference docs — not part of the shipped project
docs/

# Local-only operational/project notes
.internal/
modeldeployment.md

# AI assistant context, skills, and session artifacts — local tooling only.
# Patterns are unanchored so they match at any depth (e.g. backend/.playwright-mcp/).
.claude/
.codex/
.agents/
.impeccable/
.superpowers/
.playwright-mcp/
CLAUDE.md
AGENTS.md
.cursor/
.aider*
.github/copilot-instructions.md
